[package]
cairo-version = "=2.7.0"
name = "zktt"
version = "0.1.0"

[cairo]
sierra-replace-ids = true

[scripts]
migrate = "sozo build && sozo migrate apply"

[dependencies]
dojo = { git = "https://github.com/dojoengine/dojo", tag = "v1.0.0-alpha.12" }
origami_random = { git = "https://github.com/dojoengine/origami.git", tag = "v1.0.0-alpha.12"}

[[target.dojo]]
